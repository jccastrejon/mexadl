package mx.itesm.mexadl.metrics;

/**
 * Holds the valid properties that can be accessed by the metrics verification's
 * contexts.
 * 
 * @author jccastrejon
 * 
 */
public enum EnvironmentProperty {
    METRICS_DATA, METRICS_SET, MESSAGER, EXPECTED_VALUE
}
